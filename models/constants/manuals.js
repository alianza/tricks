const MANUALS = {
  regular: 'regular-manual',
  nose: 'nose-manual',
  switch: 'switch-manual',
  fakie: 'fakie-manual',
};

const MANUALS_ENUM = Object.values(MANUALS);

const DEFAULT_MANUAL = MANUALS.regular;

export { MANUALS_ENUM, MANUALS, DEFAULT_MANUAL };

export default MANUALS;
